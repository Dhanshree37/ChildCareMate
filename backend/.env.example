# Database Configuration
DB_HOST=           # Database host, usually 'localhost' for local setup
DB_USER=           # Your database username (e.g., 'root')
DB_PASSWORD=       # Your database password
DB_NAME=           # The name of your database (e.g., 'autism_app')

# JWT Secret
JWT_SECRET=        # A long, secure random string used to sign JWT tokens